{% extends 'layout/minitool.html.twig' %}

{% set page_icon     = 'ruler' %}
{% set page_title    = 'Distance between sequences' %}
{% set page_subtitle = 'Comparison of oligonucleotide composition' %}
{% set has_result    = oligo_array is not empty %}

{% block intro %}
    <ul>
        <li>This tool will compute distance between input sequences and UPGMA clustering will be applied.</li>
        <li>Distances are shown in a table, and a dendrogram is displaied.</li>
        <li>Check carefully the results. Their value for phylogenetics is limited.</li>
    </ul>
{% endblock %}

{% block form %}
    {{ form_start(form, {'attr': {'class': 'bp-form bp-form--single'}}) }}
        <div class="bp-form__col">
            {{ form_errors(form) }}
            {{ form_widget(form.seq) }}

            <div class="bp-form__group">
                <div class="bp-form__group-head">
                    {{ form_widget(form.method.0) }}
                    {{ form_label(form.method.0) }}
                </div>
            </div>

            <div class="bp-form__group">
                <div class="bp-form__group-head">
                    {{ form_widget(form.method.1) }}
                    {{ form_label(form.method.1) }}
                </div>
                {{ form_widget(form.len) }}
            </div>

            {{ form_widget(form.submit) }}
        </div>
    {{ form_end(form) }}
{% endblock %}

{% block result_lead %}Distances, clustering and dendrogram{% endblock %}

{% block result %}
    <div class="bp-tablewrap">
        <table>
            <thead>
                <tr>
                    <th>Distances</th>
                    {% for i in 1..oligo_array|length %}
                        <th>{{ loop.index }}</th>
                    {% endfor %}
                </tr>
            </thead>
            <tbody>
                {% for key, val in data %}
                    <tr>
                        <th>{{ key }}</th>
                        {% for i in 1..oligo_array|length %}
                            <td>{{ data[key][i] is defined ? data[key][i] : '' }}</td>
                        {% endfor %}
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>

    <h5>Name of sequences</h5>
    <pre>{% for n, name in seq_names %}{{ name }}
{% endfor %}</pre>

    <p>Length of oligoncleotides for comparison : {{ length }}</p>
    <p>Clustering method: UPGMA<br />{{ textcluster }}</p>

    <div class="bp-figure">
        <img src="/created_files/{{ dendogram_file }}?{{ "now"|date("U") }}" alt="UPGMA dendrogram" />
    </div>
{% endblock %}

{% block result_aside %}
    <div class="bp-panel">
        <h3 class="bp-panel__title">Informations</h3>
        <dl class="bp-facts">
            <dt>Sequences</dt><dd>{{ oligo_array|length }}</dd>
            <dt>Oligo length</dt><dd>{{ length }}</dd>
            <dt>Clustering</dt><dd>UPGMA</dd>
        </dl>
    </div>

    <div class="bp-panel">
        <h3 class="bp-panel__title">Export</h3>
        <a class="bp-download" href="/created_files/{{ dendogram_file }}" download>
            <svg aria-hidden="true"><use href="#bp-i-download"></use></svg>
            <span>Télécharger le dendrogramme</span>
        </a>
    </div>
{% endblock %}

{# cet outil n'a pas de texte de description dans la version d'origine #}
{% block description_step %}{% endblock %}
