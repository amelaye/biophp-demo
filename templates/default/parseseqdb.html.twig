{% extends 'layout/sample.html.twig' %}
{% import 'macros.html.twig' as macros %}

{% set page_icon  = 'database' %}
{% set page_title = 'doc.common.insert_a_sequence_into_a_database_and'|trans %}

{% block result %}
    <div class="card border-info mb-3" style="font-size:0.9em">
        <div class="card-header">{{ 'step.results_of_request'|trans }}</div>
        <div class="card-body">
            {{ macros.sequence_details(sequence) }}
        </div>
    </div>

    <div class="card border-success mb-3" style="font-size:0.9em">
        <div class="card-header">{{ 'step.results_of_request'|trans }}</div>
        <div class="card-body">
            <p>{{ 'doc.parseseqdb.new_in_biophp_1_1_gc_content'|trans }}</p>
            {{ macros.render_value(gcContent) }}
        </div>
    </div>
{% endblock %}
